# Database Configuration
# For PostgreSQL (production):
# DATABASE_URL=postgresql://username:password@localhost:5432/connector_framework_db

# For SQLite (development - default):
DATABASE_URL=sqlite:///./connector_framework.db

# Application Configuration
DEBUG=false
APP_NAME=Connector Framework Manager

# OAuth Configuration (to be added when implementing OAuth)
# OAUTH_CLIENT_ID=your_oauth_client_id
# OAUTH_CLIENT_SECRET=your_oauth_client_secret
# OAUTH_REDIRECT_URI=http://localhost:3001/auth/callback
